<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>mdrobot_gpio</name>
  <version>1.3.0</version>
  <description>
    Indicator LEDs on the Pi's own header pins. The machine is watched from
    across a room while it drives itself under a vehicle, and whether the
    camera is seeing the plate could only be read off a topic — which is no use
    standing next to it. Driven from here rather than through the RC bridge, so
    a lamp never shares a frame with the outputs that turn a drill on.
  </description>
  <maintainer email="taesuyim.kopo@gmail.com">Taesu Yim</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_python</buildtool_depend>

  <exec_depend>rclpy</exec_depend>
  <exec_depend>std_msgs</exec_depend>
  <exec_depend>geometry_msgs</exec_depend>
  <!-- lgpio has no rosdep key, so it is an apt line in the README instead:
       sudo apt install python3-lgpio
       Declaring a key that does not resolve only adds noise to every CI log.
       Same treatment as tesseract-ocr-kor in mdrobot_plate_ocr. -->

  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
